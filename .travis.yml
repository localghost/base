sudo: required

language: cpp

services: docker

install:
   - docker build -t base:travis -f docker/Dockerfile.travis docker/

script:
   - docker run --rm -v `pwd`:/opt/sources/base base:travis /bin/bash -c "cd /opt/sources/base && mkdir build && cd build && cmake ../ && make"
